[package]
name = "tesst"
version = "0.1.0"
edition = "2021"

[dependencies]
rocket = { version = "0.5", features = ["json"] }
diesel = { version = "2.0", features = ["postgres", "chrono", "r2d2"] }
dotenv = "0.15"
chrono = "0.4.38"
cron = "0.12.1"
